/*'use strict';

describe('Controller: EsriSearchCtrl', function () {

  // load the controller's module
  beforeEach(module('bloodDonationApp'));

  var EsriSearchCtrl, scope;

  // Initialize the controller and a mock scope
  beforeEach(inject(function ($controller, $rootScope) {
    scope = $rootScope.$new();
    EsriSearchCtrl = $controller('EsriSearchCtrl', {
      $scope: scope
    });
  }));

  it('should ...', function () {
    expect(1).toEqual(1);
  });
});*/
